{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "next build",
        "ignoreCommand": "echo 'Ignoring build errors'"
      }
    }
  ]
}
